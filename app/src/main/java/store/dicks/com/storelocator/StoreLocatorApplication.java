package store.dicks.com.storelocator;

import android.app.Application;
import android.content.Context;
import android.text.TextUtils;

import com.android.volley.Request;
import com.android.volley.RequestQueue;
import com.android.volley.toolbox.Volley;

/**
 * Created by vinay on 12/21/17.
 */

public class StoreLocatorApplication extends Application {

    private static final String TAG = StoreLocatorApplication.class.getSimpleName();
    public static StoreLocatorApplication INSTANCE;
    private RequestQueue mRequestQueue;

    public StoreLocatorApplication() {
        super();
        INSTANCE = this;
    }
    public static Context getContext()
    {
        return INSTANCE;
    }

    public RequestQueue getRequestQueue() {
        if (mRequestQueue == null) {
            mRequestQueue = Volley.newRequestQueue(getApplicationContext());
        }

        return mRequestQueue;
    }
    public <T> void addToRequestQueue(Request<T> req, String tag) {
        // set the default tag if tag is empty
        req.setTag(TextUtils.isEmpty(tag) ? TAG : tag);
        getRequestQueue().add(req);
    }

    public <T> void addToRequestQueue(Request<T> req) {
        req.setTag(TAG);
        getRequestQueue().add(req);
    }

    public void cancelPendingRequests(Object tag) {
        if (mRequestQueue != null) {
            mRequestQueue.cancelAll(tag);
        }
    }

}
